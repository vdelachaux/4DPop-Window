name: Build
on:
  push:
    paths:
      - '**.4dm'
      - '**.yml'
  pull_request:
    paths:
      - '**.4dm'
  workflow_dispatch:

jobs:
  build:
    name: "Build on ${{ matrix.os }}"
    strategy:
      fail-fast: false
      matrix:
        os: [ macos-latest, windows-latest ]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Get dependencies from BUILD zip (macOS)
      if: runner.os == 'macOS'
      shell: bash
      run: |
        set -euo pipefail
        mkdir -p Components
        curl -fL "https://github.com/vdelachaux/Color-with-Classes/raw/master/BUILD/Color-with-Classes.zip" -o "Color-with-Classes.zip"
        curl -fL "https://github.com/vdelachaux/SVG-with-Classes/raw/master/BUILD/SVG-with-Classes.zip" -o "SVG-with-Classes.zip"
        curl -fL "https://github.com/vdelachaux/4DPop/raw/master/Build/4DPop.zip" -o "4DPop.zip"

        rm -rf "Components/Color-with-Classes.4dbase" "Components/SVG with Classes.4dbase" "Components/4DPop.4dbase"
        unzip -o "Color-with-Classes.zip" -d Components
        unzip -o "SVG-with-Classes.zip" -d Components
        mkdir -p "Components/4DPop.4dbase"
        unzip -o "4DPop.zip" -d "Components/4DPop.4dbase"
        rm -rf "Components/__MACOSX"
        rm -f *.zip
    - name: Get dependencies from BUILD zip (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Path Components -Force | Out-Null
        Invoke-WebRequest -Uri "https://github.com/vdelachaux/Color-with-Classes/raw/master/BUILD/Color-with-Classes.zip" -OutFile "Color-with-Classes.zip"
        Invoke-WebRequest -Uri "https://github.com/vdelachaux/SVG-with-Classes/raw/master/BUILD/SVG-with-Classes.zip" -OutFile "SVG-with-Classes.zip"
        Invoke-WebRequest -Uri "https://github.com/vdelachaux/4DPop/raw/master/Build/4DPop.zip" -OutFile "4DPop.zip"

        Remove-Item -Recurse -Force "Components/Color-with-Classes.4dbase" -ErrorAction SilentlyContinue
        Remove-Item -Recurse -Force "Components/SVG with Classes.4dbase" -ErrorAction SilentlyContinue
        Remove-Item -Recurse -Force "Components/4DPop.4dbase" -ErrorAction SilentlyContinue

        Expand-Archive -Path "Color-with-Classes.zip" -DestinationPath "Components" -Force
        Expand-Archive -Path "SVG-with-Classes.zip" -DestinationPath "Components" -Force
        New-Item -ItemType Directory -Path "Components/4DPop.4dbase" -Force | Out-Null
        Expand-Archive -Path "4DPop.zip" -DestinationPath "Components/4DPop.4dbase" -Force
        Remove-Item -Path "*.zip" -Force -ErrorAction SilentlyContinue
    - name: Build
      uses: 4d/build4d-action@main
      with:
        product-line: vcs
        version: vcs
        build: official
        token: ${{ secrets.DLTK }}